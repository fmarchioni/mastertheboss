server:
    port: 8080

spring:
    datasource:
        url: jdbc:h2:mem:mydb
        username: sa
        password: sa
    jpa:
        open-in-view: false
        properties:
            hibernate:
                search:
                    backend:
                        type: lucene
                        directory.root: ./data/index